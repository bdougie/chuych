<h1>Churches</h1>
<p>Dont see the church you are looking for? Try creating it.</p>

<div class='pull-right'>
	<%= link_to "Create a new church", new_church_path, class: "btn btn-primary" %>
</div>

<div>
	<%= render 'search_form' %>
</div>

<div>
	<%= render partial: 'churches/churches', locals: {churches: @churches} %>
</div>


<hr/><br/>
	<%= will_paginate @churches %>
